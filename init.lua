-- bootstrap lazy.nvim, LazyVim and your plugins
require("config.lazy")

-- The colorscheme is restored by LazyVim's `colorscheme` option, see
-- lua/plugins/colorscheme.lua and lua/utils/persist.lua.

-- helper tool to make transparent background
-- require("utils.bg").clear_bg()
